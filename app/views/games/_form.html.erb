<%= form_for(game) do |f| %>
  <% if game.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(game.errors.count, "error") %> prohibited this game from being saved:</h2>

      <ul>
      <% game.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :player_red_id %>
    <%= f.number_field :player_red_id %>
  </div>

  <div class="field">
    <%= f.label :player_blue_id %>
    <%= f.number_field :player_blue_id %>
  </div>

  <div class="field">
    <%= f.label :games_state %>
    <%= f.text_area :games_state %>
  </div>

  <div class="field">
    <%= f.label :red_turn %>
    <%= f.check_box :red_turn %>
  </div>

  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
